---
description: Manage codegraph background daemons
argument-hint: "[path]"
skills: codegraph-helper
---

Use the `codegraph-helper` skill for this request:

Run `codegraph daemon` in `$ARGUMENTS` (default `.`). Lists running codegraph background daemons — pick one to stop. Interactive — does not auto-select.

Boundaries: read-only.